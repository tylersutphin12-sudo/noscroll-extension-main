# NoScroll Icons

1. Add your logo as `assets/icon.png` (recommend dark background for toolbar visibility).
2. Run `npm run generate-icons` from the project root to create icon sizes (16, 32, 48, 128) in `assets/`.
